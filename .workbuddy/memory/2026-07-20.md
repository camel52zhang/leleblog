# leleblog 每日工作日志 — 2026-07-20

## 一、项目分析（17 项待优化，按严重度）
- 🔴 高：(1) 默认管理员 admin/admin；(2) JWT_SECRET 缺失静默回退硬编码明文；(3) CORS `*` + 死配置 ALLOWED_ORIGINS；(4) 备份恢复直接覆盖线上 SQLite；(5) 备份导出读线上 SQLite。
- 🟠 中：(6) 全局单 limiter 非按 IP；(7) 孤儿 backend/models/post.go；(8) 692 行 main.go god file；(9) admin 评论接口无分页；(10) next.config.ts 硬编码 leleblog-api:7070；(11) 上传 URL 用 c.Request.Host + 硬编码 http；(12) 无优雅关闭。
- 🟡 低：(13) 代码高亮仅后台编辑器、公开页纯文本；(14) README 是默认模板；(15) 缺 .dockerignore/.gitignore/data 忽略；(16) 生产镜像偏重；(17) 无测试/CI/healthcheck。

## 二、全部实施后结果
### 后端（#1–#12, #17 部分）
- 拆 692 行 main.go 为 6 文件 package main：models.go / middleware.go / utils.go / backup.go / handlers.go / main.go。
- #1 弱口令：默认不再 admin/admin，改为随机密码 + 首次强制改密(MustChangePwd)。
- #2 JWT：JWT_SECRET 缺失即 panic 拒启动(fail-fast)，无硬编码明文。
- #3 CORS：`*` → 白名单，仅回显已配置合法 Origin 并允许凭据。
- #4/#5 备份：导出 VACUUM INTO 一致性快照；恢复 关连接→备份旧库→原子重命名→重连+AutoMigrate。
- #6 限流：全局单例 → 按客户端 IP（尊重 X-Forwarded-For）。
- #7 删孤儿 backend/models/post.go。
- #9 评论分页：/api/admin/comments 加 page/pageSize/total。
- #11 上传路径：返回相对 /uploads/xxx。
- #12 优雅关闭：http.Server + signal.Notify(SIGINT/SIGTERM)。
- #17 探活：/api/health（ping 数据库）。

### 前端（#10, #13）
- #13 代码高亮：公开文章页 + 后台编辑器统一接入 react-syntax-highlighter（Prism oneDark）；原两页其实都只渲染纯文本、后台 oneLight 是死导入，已修。
- #10 强制改密：登录返回 must_change_password 时跳新建 /admin/change-password 页。

### 工程化（#14, #15, #16, #17）
- #16 镜像：Dockerfile 多阶段 alpine，构建命令 main.go → `.`（多文件必须编整包）。
- #15/.gitignore：新建根 .gitignore + backend/.dockerignore（忽略 .env、*.db、uploads/）。
- #14 README：从无重写为实用文档。
- #17 工程化：.env.example、compose 注入 JWT_SECRET + healthcheck、首个 Go 单测 utils_test.go、GitHub Actions CI（后端 CGO 构建+测试 / 前端 tsc+build）。

### 验证方式
本地无 gcc，临时切纯 Go 版 glebarez/sqlite（API 一致）做 go vet + go test（exit 0）、前端 tsc --noEmit 通过，再还原 CGO 驱动 gorm.io/driver/sqlite 并恢复 go.mod/go.sum。

## 三、Docker 端到端验收 + 修 2 个真 bug
- 后端镜像 camel52zhang/leleblog-api:latest(46.5MB) 在 Docker 内 gcc+CGO 编译成功（本地无 gcc 没法测的关键点）。
- 验收通过：/api/health ok；前端 :4000/api/* 代理转发正常；带 token 访问 admin 接口 OK、无 token 401；CORS 白名单合法回显/非法不回显；登录成功。
- Bug1：compose 漏传 ADMIN_PASSWORD（#1 收尾漏洞），.env 配了没透传 → 后端走随机密码导致登录对不上。已加 ADMIN_PASSWORD=${ADMIN_PASSWORD}。
- Bug2：改密接口用 Updates(map{"must_change_password":false})，但 DB 列名是 must_change_pwd（must_change_password 只是 json tag），整句因 no such column 失败且没查 err → 改密假成功、新密码没存。已改 db.Model(&user).Select("PasswordHash","MustChangePwd").Updates(user) 并校验 err，重建重测通过。
- 后续：把改密链路做成 CI 集成测试 changepassword_test.go（守护 Bug2 回归），并新增 docker-smoke 任务复刻端到端验收；CI YAML 已用 pyyaml 校验。

## 四、现状（截至 15:43）
全栈运行中，管理员密码已对齐 .env 的 ADMIN_PASSWORD。leleblog-api Up(healthy)、leleblog-web Up 42min、data/leleblog.db 在位。
